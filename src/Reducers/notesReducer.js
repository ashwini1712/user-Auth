const initialNotes = []

const notesReducer = (state = initialNotes , action) =>{
    switch(action.type){
        
        default:{
            return state
        }
    }
}

export default notesReducer